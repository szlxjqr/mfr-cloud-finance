# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-verify
dist-ssr
*.local

# pdfjs 资源由构建脚本从 node_modules 生成，不纳入版本控制
public/cmaps/
public/standard_fonts/

# tesseract 离线 OCR：worker + wasm 核心随构建发布（入库）；
# 中文字库（chi_sim/eng .traineddata.gz）体积大，由 `npm run setup:ocr`
# 一次性拉取到 public/tesseract/lang/，不入库（见 scripts/fetch-tesseract-lang.mjs）
public/tesseract/lang/

# 发票解析离线回归测试现场编译产物（由 tests/invoice_accuracy.mjs 生成）
.invoice-test-build/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
