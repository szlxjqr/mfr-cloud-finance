# 云财务 — 开发日志 (DevLog)

> 记录项目从 0 到 1 的完整开发过程。所有改动均保存于 `/workspace/mfr-cloud-finance/`。

---

## 环境信息
- 系统：Ubuntu 22.04 (sandbox)
- Node：v22.13.0 / Python 3.11
- 预览端口：前端 8137（静态 `dist`），后端 8000

---

## Session 1 — 初始化与技能加载
**日期**：2026-07-18

- 加载 3 个财务技能：`accountant-expert`、`accountant`、`financial-reporting-dashboard`
- 创建项目目录结构：`mfr-cloud-finance/{frontend,backend,docs,config,data,scripts,src}`
- 初始化 Vue3+Vite+TS 前端，安装 Element Plus / ECharts / Pinia / Vue Router / Axios
- 初始化 FastAPI 后端，配置 CORS

**产出文件**：
- `frontend/` 脚手架 + `backend/app/` 基础结构

---

## Session 2 — 整体布局与首页仪表盘（Phase 1）
**参考**：WP5云会计 截图

- 实现 `TopBar.vue`（顶部导航栏）
- 实现 `SideNav.vue`（左侧 11 项菜单）
- 实现 `MainLayout.vue`（主布局容器）
- 实现仪表盘四大区块组件：
  - `QuickActions.vue`（12 个快捷功能）
  - `VoucherCard.vue`（凭证中心）
  - `FundOverview.vue`（资金情况 6 指标）
  - `BusinessChart.vue`（经营数据折线图）
  - `TaxChart.vue`（应交税费环形图）
- 后端 4 个 Mock 数据接口

**关键修复**：
- `style.css` 的 `#app` 被脚手架默认模板限制为 `width:1126px` → 改为 `width:100%` 解决主页宽度不够
- `MainLayout.vue` 的 `.main-right` 缺少 `flex:1` → 补充后主内容区撑满

---

## Session 3 — 总账三级子菜单
**参考**：总账菜单截图

- `SideNav.vue` 重构为支持嵌套 `el-sub-menu`
- 总账下设 3 大分组：
  - **凭证管理**：凭证 / 查看凭证 / 原始凭证
  - **账簿**：总账 / 明细账 / 余额表 / 序时账 / 多栏账 / 科目汇总表
  - **辅助账簿**：科目辅助明细账/余额表、数量外币明细账/余额表、核算项目明细账/余额表
- `router/index.ts` 注册全部 15 个子路由（占位页 `Placeholder`，带开发中提示）

---

## Session 4 — 凭证录入页
**参考**：记账凭证录入截图

- 新建 `views/general-ledger/Voucher.vue`
- 工具栏：搜索、保存(Ctrl+S)、保存并新增(F5)、保存并打印、清除、更多下拉、导航箭头
- 凭证头：凭证字(记/收/付/转)、号、日期、期间、附单据数
- 分录表：序号/摘要/科目/借方(14位金额列)/贷方(14位金额列)
- 合计行 + 借贷平衡校验 + 底部状态栏
- 路由指向真实组件

---

## Session 5 — 查看凭证页
**参考**：查看凭证列表截图

- 新建 `views/general-ledger/VoucherList.vue`
- 工具栏：日期/期间切换、日期范围、搜索、筛选、刷新、展开分录、取消分页、新增、导入、电子账簿、整理编号
- 批量栏：已选 N 条、审核/打印/导出下拉、删除
- 表格：选择框(el-table 原生 selection)、展开箭头、摘要(凭证字+号/日期/状态)、科目、借/贷金额
- 展开明细：分录明细表 + 制单人/审核人/附单据/备注
- 内置 5 条 Mock 凭证（记/收/付字、草稿与已审状态）
- 分页栏（取消分页可切换）

**修复**：`type="width"` 非 el-table 合法列类型 → 改用原生 `type="selection"`；清理未使用变量通过 TS 校验

---

## Session 6 — 原始凭证管理页
**参考**：原始凭证截图

- 新建 `views/general-ledger/OriginalVoucher.vue`
- 左侧分类面板：全部分类 / 凭证附件 / 发票 / 银行回单（带计数）
- 顶部工具栏：日期/期间切换、日期范围、搜索附件名称、筛选下拉、刷新、上传附件
- 批量操作栏：已选 N 条、生成凭证、关联凭证、修改、删除、下载
- 表格列：选择框、附件（文件类型徽章+名称+大小）、附件说明及备注、凭证编号、凭证状态、操作
- 内置 5 条 Mock 附件数据（PDF/JPG、已关联/未关联凭证、不同分类）
- 路由指向真实组件

---

## Session 7 — 账簿核心页面（总账/明细账/余额表）
**日期**：2026-07-18

- 新建 `views/general-ledger/GeneralLedger.vue` — 总账
  - 科目树结构，支持展开/收起子科目（如银行存款→基本户/一般户）
  - 期初余额（借/贷）、本期发生额（借/贷）、期末余额（借/贷）六列
  - 科目级别筛选（一级/二级）、关键词搜索、含无发生额开关
  - 全部展开/收起按钮，联查明细账
  - 底部合计栏汇总全部科目
  - 17个科目的完整 Mock 数据（资产/负债/权益/成本/损益全覆盖）

- 新建 `views/general-ledger/DetailLedger.vue` — 明细账
  - 科目选择器弹窗（19个科目可搜索切换）
  - 分录流水表：日期/凭证字号/摘要/对方科目/借方/贷方/余额
  - 联查凭证（跳转到查看凭证页）
  - 前后期间切换按钮，含未记账开关
  - 底部本期合计 + 期末余额统计

- 新建 `views/general-ledger/BalanceSheet.vue` — 科目余额表
  - 比总账多"本年累计"列（借方/贷方）
  - 期初/本期发生/本年累计/期末 四组八列
  - 23个科目含二级展开，科目类别着色
  - 联查明细账和总账
  - 底部四组统计卡片式合计栏

- 路由更新：`GeneralLedger`、`DetailLedger`、`BalanceSheet` 从 Placeholder 替换为真实组件

---

## Session 8 — 账簿模块剩余页面（序时账/多栏账/科目汇总表）
**日期**：2026-07-18

- 新建 `views/general-ledger/ChronologicalLedger.vue` — 序时账
  - 按时间顺序排列所有凭证分录（45条 Mock 数据）
  - 日期/期间切换、状态筛选（草稿/已审/已记账）
  - 联查凭证和明细账
  - 底部借贷平衡校验

- 新建 `views/general-ledger/ColumnarLedger.vue` — 多栏账
  - 支持管理费用(7栏)/销售费用(5栏)/生产成本(4栏)三种模板
  - 动态分栏显示 + 借方/贷方/余额合计列
  - 底部栏目合计行

- 新建 `views/general-ledger/AccountSummary.vue` — 科目汇总表
  - 按科目汇总本期及本年累计借贷发生额
  - 18个科目含展开，底部四张统计卡片

- 路由更新：三个页面从 Placeholder 替换为真实组件
- **账簿模块 6 个页面全部完成** ✓

---

## 当前代码状态
```
源码文件：33 个 (.vue / .ts，不含后端)
构建产物：frontend/dist/ 已生成
预览服务：前端 8137（HTTP 200）、后端 8000（HTTP 200）
一级菜单：10 个（首页/总账/报表/出纳/资产/工资/发票/结账/设置/新手指引）
子路由：47 个（全部注册）
已落地页面：16 个完整业务页（仪表盘1+凭证3+账簿6+辅助6）+ 18 个占位页（报表5/出纳3/资产1/工资7/发票5/结账2/设置12）
```

## 待办
- [x] 侧边栏菜单分组化（报表/出纳/资产/工资/发票/结账/设置全部分组）✓ Session 15-20
- [x] 删除营销元素（业财一体菜单 + 底部联系客服/领取资料）✓ Session 21
- [x] 替换为公司 Logo ✓ Session 21
- [ ] 报表模块 5 页实现（资产负债表/利润表/利润表季报/现金流量表/现金流量表季报）
- [ ] 出纳模块 3 页实现（日记账/业务类型/核对总账）
- [ ] 资产模块 1 页实现（固定资产管理）
- [ ] 工资模块 7 页实现
- [ ] 发票模块 5 页实现
- [ ] 结账模块 2 页实现（期末结转/结账）
- [ ] 设置模块 12 页实现
- [ ] 复式记账引擎（借贷校验后端化）
- [ ] 凭证真实持久化（当前为 Mock 数据）

---

## Session 9 — 科目辅助明细账（辅助账簿首页面）
**日期**：2026-07-18

- 新建 `views/general-ledger/AuxDetailLedger.vue` — 科目辅助明细账（左右分栏）
  - 左侧辅助核算项目列表（客户/供应商/部门/人员共12项，可搜索）
  - 右侧超宽表格：
    - 固定列：辅助核算编码 / 辅助核算名称
    - 日期 / 凭证号 / 摘要
    - 借方（数量 / 单价 / 金额 子组）
    - 贷方（数量 / 单价 / 金额 子组）
    - 方向（el-tag）
    - 科目余额（数量 / 单价 / 金额 子组）
  - 顶部工具栏：期间 / 筛选 / 显示封存科目 / 打印 / 导出
  - 选择器带 ✕ 关闭按钮 + 币别下拉（本币 / USD / EUR）
  - 期初余额行 + 随机业务分录（借贷数量联动、余额滚动）
- 路由更新：`general-ledger/aux-detail` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- **辅助账簿 1/6 页面完成** ✓


---

## Session 10 — 科目辅助余额表（辅助账簿第2页面）
**日期**：2026-07-18

- 新建 `views/general-ledger/AuxBalanceSheet.vue` — 科目辅助余额表（左右分栏）
  - 左侧辅助核算项目列表（客户/供应商/部门/人员共12项，可搜索）
  - 右侧超宽表格（余额汇总型）：
    - 固定列：辅助核算编码 / 辅助核算名称
    - 计量单位
    - **期初余额**（数量 / 单价 / 借方金额 / 贷方金额）
    - **本期发生额**（借方数量 / 借方金额 / 贷方数量 / 贷方金额）
    - **期末余额**（数量 / 单价 / 借方金额 / 贷方金额）
  - 顶部工具栏：期间范围(monthrange) / 筛选 / 显示封存科目 / 隐藏余额为0 / 打印 / 导出
  - 选择器带 ◀◀ ▶▶ 四向导航 + ✕ 关闭按钮 + 币别下拉（本币 / USD / EUR）
  - 支持隐藏余额为0过滤 + el-table show-summary 合计行
- 路由更新：`general-ledger/aux-balance` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- **辅助账簿 2/6 页面完成** ✓


---

## Session 11 — 数量外币明细账（辅助账簿第3页面）
**日期**：2026-07-18

- 新建 `views/general-ledger/QtyFxDetaiLedger.vue` — 数量外币明细账（左右分栏）
  - 左侧科目树（8个科目含子节点：库存现金/银行存款/应收账款/原材料/库存商品等）
  - 右侧表格：
    - 日期 / 凭证号 / 摘要
    - **借方**（数量 / 单价 / 金额 子组，根据模式动态显隐）
    - **贷方**（数量 / 单价 / 金额 子组，同上）
    - **余额**（方向 el-tag + 数量 / 单价 / 余额金额 子组）
  - 表格上方选项行（截图特征）：
    - 显示明细科目名称（checkbox）
    - 显示方式（checkbox）
    - 数量金额模式下拉（数量金额 / 仅数量 / 仅金额 / 原币本位）→ 切换时动态控制列显隐
  - 顶部工具栏：期间范围(monthrange) / 筛选 / 刷新 / 打印 / 导出
  - 选择器 ◀ | 请选择科目 | ▶（无 ✕ 按钮，与截图一致）
- 路由更新：`general-ledger/qty-fx-detail` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- **辅助账簿 3/6 页面完成** ✓


---

## Session 12 — 数量外币余额表（辅助账簿第4页面）
**日期**：2026-07-18

- 新建 `views/general-ledger/QtyFxBalanceSheet.vue` — 数量外币余额表（左右分栏）
  - 左侧科目树（8个科目含子节点：库存现金/银行存款/应收账款/原材料/库存商品等）
  - 右侧超宽表格（余额汇总型，与科目辅助余额表类似但固定列为科目编号/名称）：
    - 固定列：科目编号 / 科目名称 + 计量单位
    - **期初余额**（数量 / 单价 / 借方金额 / 贷方金额，根据模式动态显隐）
    - **本期发生额**（借方数量 / 借方金额 / 贷方数量 / 贷方金额）
    - **期末余额**（数量 / 单价 / 借方金额 / 贷方金额）
  - 表格上方选项行（截图特征）：
    - 显示方式（文本标签）
    - 数量金额模式下拉（4种模式 → 动态控制所有组内子列显隐）
    - 隐藏余额为0（checkbox 过滤行）
  - 顶部工具栏：期间范围(monthrange) / 筛选 / 刷新 / 打印 / 导出
  - 选择器 ◀◀ | 请选择科目 | ▶▶ 四向导航（双箭头）
  - 支持隐藏余额为0过滤 + el-table show-summary 合计行
- 路由更新：`general-ledger/qty-fx-balance` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- **辅助账簿 4/6 页面完成** ✓


---

## Session 13 — 核算项目明细账（辅助账簿第5页面）
**日期**：2026-07-18

- 新建 `views/general-ledger/ProjectDetailLedger.vue` — 核算项目明细账（左右分栏）
  - 左侧辅助类别列表（7类：客户/供应商/部门/员工/专项/存货/其他），蓝色选中态
  - 搜索框 placeholder「请输入要搜索的辅助类别」
  - 右侧表格列：日期 / 科目名称 / 辅助项 / 凭证号 / 摘要 / 借方 / 贷方 / 方向(el-tag) / 余额
  - 选择器行：◀ | 客户 | ▶ + **辅助项：全部辅助项** ▾ + **科目：全部科目** ▾
  - 表格上方选项行：显示方式（请选择显示方式，4种模式可选）
  - 顶部工具栏：期间范围(monthrange) / 筛选 / 刷新 / 打印 / 导出
  - 每个类别含独立数据集：
    - 客户(4项+3科目)、供应商(4项+4科目)、部门(5项+3科目)
    - 员工(6项+3科目)、专项(3项+3科目)、存货(4项+4科目)、其他(3项+3科目)
  - 支持按辅助项和科目双重筛选过滤
- 路由更新：`general-ledger/project-detail` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- **辅助账簿 5/6 页面完成** ✓


---

## Session 14 — 核算项目余额表（辅助账簿第6页面，模块收官）
**日期**：2026-07-18

- 新建 `views/general-ledger/ProjectBalanceSheet.vue` — 核算项目余额表（左右分栏）
  - 左侧辅助类别列表（7类：客户/供应商/部门/员工/专项/存货/其他），蓝色选中态
  - 右侧超宽表格（四组八列+可隐藏本年累计）：
    - 固定列：**项目编号** / **项目名称**
    - **期初余额**（借方金额 / 贷方金额）
    - **本期发生额**（借方金额 / 贷方金额）
    - **本年累计发生额**（借方金额 / 贷方金额，**v-if 可隐藏**）
    - **期末余额**（借方金额 / 贷方金额）
  - 顶部工具栏 3 个 checkbox：只显示一级科目 / 隐藏余额为0 / 隐藏本年累计
  - 选择器行：◀◀ | 客户 | ▶▶ 四向导航 + 辅助项下拉 + 科目下拉
  - 表格上方选项行：显示方式（请选择下拉）
  - 每个类别含独立项目数据集（含项目编号 code）：
    - 客户(KM001-KM004,4项) / 供应商(GY001-GY004,4项) / 部门(DP001-DP005,5项)
    - 员工(EP001-EP006,6项) / 专项(PJ001-PJ003,3项) / 存货(IV001-IV004,4项) / 其他(OT001-OT003,3项)
  - 支持隐藏余额为0 + 隐藏本年累计列 + show-summary 合计行
- 路由更新：`general-ledger/project-balance` 由 Placeholder 改为真实组件
- 构建通过（`npm run build` ✓），已部署至 8137 预览服务
- 🎉 **辅助账簿 6/6 页面全部完成！** 账簿模块总计 12 个页面全部实现


---

## Session 15-21 — 侧边栏菜单分组重构 + 品牌升级

### Session 15 — 报表模块菜单框架
- SideNav.vue：「报表」从普通一级菜单 → `el-sub-menu` 绿色分组「财务报表」
  - 5 子项：资产负债表 / 利润表 / 利润表季报 / 现金流量表 / 现金流量表季报
- router：新增 reports/ 下 5 个 Placeholder 子路由；创建 views/reports/

### Session 16 — 出纳菜单分组
- 「出纳」→ 紫色「资金管理」分组：日记账 / 业务类型 / 核对总账（3 项）
- 路由：cashier/ 下 3 子路由；views/cashier/

### Session 17 — 资产菜单分组
- 「资产」→ 青色「固定资产」分组：固定资产管理（1 项）
- 路由：assets/fixed-asset；views/assets/

### Session 18 — 工资菜单双分组（两列布局）
- 「工资」→ 双分组左右两列：
  - 绿色「工资」：员工基本信息 / 工资列表 / 部门工资汇总表 / 个税报表
  - 蓝色「工资设置」：社保及公积金设置 / 工资计算设置 / 工资分摊
- 路由：payroll/ 下 7 子路由；views/payroll/；新增 .payroll-grid 两列网格样式

### Session 19 — 发票菜单双分组（两列布局）
- 「发票」→ 双分组左右两列：
  - 蓝色「发票」：进项发票 / 销项发票 / 费用发票
  - 橙色「发票设置」：发票抬头 / 发票设置
- 路由：invoice/ 下 5 子路由；views/invoice/

### Session 20 — 结账菜单分组 + 设置菜单双分组
- 「结账」→ 橙色「结账」：期末结转 / 结账（2 项）；closing/ 2 路由
- 「设置」→ 双分组左右两列：
  - 蓝色「基础数据」：科目/期初/辅助字典/币别/凭证摘要/凭证字（6）
  - 橙色「基础设置」：凭证模板/凭证配置/日记账模板/归档管理/系统设置/审计接口文件导出（6）
- 路由：settings/ 下 12 子路由；views/settings/

### Session 21 — 菜单清理 + 品牌升级
- 删除「业财一体」营销菜单项（橙色高亮）
- 删除底部「联系客服 / 领取资料」功能区 + Powered by MFR云
- 清理 handleFooter / ElMessage / menu-highlight 等无用代码
- 替换侧边栏 Logo：文字 "M" → 公司 logo.png（紫蓝渐变 M 形图标，30×30px）

**阶段成果汇总：**
- 侧边栏 10 个一级菜单全部完成分组化（除首页/新手指引为普通项）
- 路由体系：总账(12) + 报表(5) + 出纳(3) + 资产(1) + 工资(7) + 发票(5) + 结账(2) + 设置(12) = 47 个子路由全部注册
- 视图目录：dashboard / general-ledger / reports / cashier / assets / payroll / invoice / closing / settings 全部就绪
- 品牌 Logo 上线，UI 更专业统一
