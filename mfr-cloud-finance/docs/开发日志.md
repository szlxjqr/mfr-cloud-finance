# MFR云财务 — 开发日志 (DevLog)

> 记录项目从 0 到 1 的完整开发过程。所有改动均保存于 `/workspace/mfr-cloud-finance/`。

---

## 环境信息
- 系统：Ubuntu 22.04 (sandbox)
- Node：v22.13.0 / Python 3.11
- 预览端口：前端 8137（静态 `dist`），后端 8000

---

## Session 1 — 初始化与技能加载
**日期**：2026-07-18

- 加载 3 个财务技能：`accountant-expert`、`accountant`、`financial-reporting-dashboard`
- 创建项目目录结构：`mfr-cloud-finance/{frontend,backend,docs,config,data,scripts,src}`
- 初始化 Vue3+Vite+TS 前端，安装 Element Plus / ECharts / Pinia / Vue Router / Axios
- 初始化 FastAPI 后端，配置 CORS

**产出文件**：
- `frontend/` 脚手架 + `backend/app/` 基础结构

---

## Session 2 — 整体布局与首页仪表盘（Phase 1）
**参考**：WP5云会计 截图

- 实现 `TopBar.vue`（顶部导航栏）
- 实现 `SideNav.vue`（左侧 11 项菜单）
- 实现 `MainLayout.vue`（主布局容器）
- 实现仪表盘四大区块组件：
  - `QuickActions.vue`（12 个快捷功能）
  - `VoucherCard.vue`（凭证中心）
  - `FundOverview.vue`（资金情况 6 指标）
  - `BusinessChart.vue`（经营数据折线图）
  - `TaxChart.vue`（应交税费环形图）
- 后端 4 个 Mock 数据接口

**关键修复**：
- `style.css` 的 `#app` 被脚手架默认模板限制为 `width:1126px` → 改为 `width:100%` 解决主页宽度不够
- `MainLayout.vue` 的 `.main-right` 缺少 `flex:1` → 补充后主内容区撑满

---

## Session 3 — 总账三级子菜单
**参考**：总账菜单截图

- `SideNav.vue` 重构为支持嵌套 `el-sub-menu`
- 总账下设 3 大分组：
  - **凭证管理**：凭证 / 查看凭证 / 原始凭证
  - **账簿**：总账 / 明细账 / 余额表 / 序时账 / 多栏账 / 科目汇总表
  - **辅助账簿**：科目辅助明细账/余额表、数量外币明细账/余额表、核算项目明细账/余额表
- `router/index.ts` 注册全部 15 个子路由（占位页 `Placeholder`，带开发中提示）

---

## Session 4 — 凭证录入页
**参考**：记账凭证录入截图

- 新建 `views/general-ledger/Voucher.vue`
- 工具栏：搜索、保存(Ctrl+S)、保存并新增(F5)、保存并打印、清除、更多下拉、导航箭头
- 凭证头：凭证字(记/收/付/转)、号、日期、期间、附单据数
- 分录表：序号/摘要/科目/借方(14位金额列)/贷方(14位金额列)
- 合计行 + 借贷平衡校验 + 底部状态栏
- 路由指向真实组件

---

## Session 5 — 查看凭证页
**参考**：查看凭证列表截图

- 新建 `views/general-ledger/VoucherList.vue`
- 工具栏：日期/期间切换、日期范围、搜索、筛选、刷新、展开分录、取消分页、新增、导入、电子账簿、整理编号
- 批量栏：已选 N 条、审核/打印/导出下拉、删除
- 表格：选择框(el-table 原生 selection)、展开箭头、摘要(凭证字+号/日期/状态)、科目、借/贷金额
- 展开明细：分录明细表 + 制单人/审核人/附单据/备注
- 内置 5 条 Mock 凭证（记/收/付字、草稿与已审状态）
- 分页栏（取消分页可切换）

**修复**：`type="width"` 非 el-table 合法列类型 → 改用原生 `type="selection"`；清理未使用变量通过 TS 校验

---

## Session 6 — 原始凭证管理页
**参考**：原始凭证截图

- 新建 `views/general-ledger/OriginalVoucher.vue`
- 左侧分类面板：全部分类 / 凭证附件 / 发票 / 银行回单（带计数）
- 顶部工具栏：日期/期间切换、日期范围、搜索附件名称、筛选下拉、刷新、上传附件
- 批量操作栏：已选 N 条、生成凭证、关联凭证、修改、删除、下载
- 表格列：选择框、附件（文件类型徽章+名称+大小）、附件说明及备注、凭证编号、凭证状态、操作
- 内置 5 条 Mock 附件数据（PDF/JPG、已关联/未关联凭证、不同分类）
- 路由指向真实组件

---

## 当前代码状态
```
源码文件：28 个 (.vue / .ts / .py)
构建产物：frontend/dist/ 已生成
预览服务：前端 8137（HTTP 200）、后端 8000（HTTP 200）
```

## 待办
- [ ] 复式记账引擎（借贷校验后端化）
- [ ] 会计科目树 + 科目选择器弹窗（凭证录入页当前为占位）
- [ ] 凭证真实持久化（当前为 Mock 数据）
- [ ] 财务报表（资产负债表/利润表/现金流量表）
- [ ] 发票管理模块
