# 后端环境变量模板（复制为 .env 后填写，.env 已被 gitignore 忽略）
# 开发默认 SQLite，无需配置即可运行；生产部署改为云 PostgreSQL（如 RDS）。

# 云数据库连接串示例：
# DATABASE_URL=postgresql://user:password@localhost:5432/smart_finance
# DATABASE_URL=sqlite:///./smart_finance.db
